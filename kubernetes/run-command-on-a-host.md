# Running a command on a Kubernetes host

In a perfect world you would never need to manually run a command on one of your orchestrator hosts, but in the real world you will probably need to do it at least once. Most orchestration systems are not designed for this operation, but they rarely block you from doing it.

[comment]: # (nodeSelector and how it differs from fleet affinity/anti-affinity)

## Running commands on a fleet host

## Running commands on a Kubernetes host
